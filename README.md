Create a file.
Download three code files (index.html, style.css, script.js) and keep them in the file.
Create a new file inside the main file and name it "assets".
Keep all the images and pdfs inside the "assets" file.
